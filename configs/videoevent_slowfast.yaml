dataset_name: videoevent
train_split: ['training']
val_split: ['validation']
dataset: {
  json_file: /data1/zst/help_TriDet/TriDet/data/videoevent/annotations/chg3_videoevent_latest.json,      #./data/videoevent/annotations/videoevent_new.json,
  # feat_folder: '/data2/video_feat_extract/slowfast_features_30fps',
  feat_folder: '/data1/data/tiktokCrawler/video_feat_extract/new_videnent_features',     #'/data1/zst/help_TriDet/slowfast_features',
  file_prefix: ,
  file_ext: .npy,
  num_classes: 1,   #433,   #502,      #1872,        #1366,                 #962, ###2   #1874
  input_dim: 2304,
  feat_stride: 16,
  num_frames: 32,
  default_fps: 30,
  trunc_thresh: 0.3,
  crop_ratio: [0.9, 1.0],
  max_seq_len: 2304,
}
model: {
  regression_range: [ [ 0, 4 ], [ 2, 8 ], [ 4, 16 ], [ 8, 32 ], [ 16, 64 ], [ 32, 10000 ] ],
  fpn_type: identity,
  max_buffer_len_factor: 4.0,
  n_sgp_win_size: 5,
  num_bins: 16,
  iou_weight_power: 0.25,
  sgp_mlp_dim: 1024,
  k: 2,
}
opt: {
  learning_rate: 0.00001,
  epochs: 500,
  weight_decay: 0.00001,
}
loader: {
  batch_size: 2,
}
train_cfg: {
  init_loss_norm: 100,
  clip_grad_l2norm: 1.0,
  cls_prior_prob: 0.01,
  center_sample: radius,
  center_sample_radius: 1.5,
}
test_cfg: {
  pre_nms_topk: 5000,
  max_seg_num: 2000,    #2000,
  min_score: 0.001,
  nms_sigma: 0.9,
  multiclass_nms: True,
}
output_folder: ./newckpt10/
